// import {isEmpty, isEmail, isBoolean, equals, contains} from 'validator';

// prefer custom validation for learning purpose instead of installed packege functions
export const signUpValidator = async ({ email, password, username }) => {
     const error = {};
}

